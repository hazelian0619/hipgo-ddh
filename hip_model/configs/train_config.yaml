# 数据配置
data:
  train_json: "labeled_data/train/dataset.json"
  val_json: "labeled_data/val/dataset.json"
  img_dir: "processed_images"
  
# 模型配置
model:
  backbone: "resnet50"
  num_classes: 1
  
# 训练配置
train:
  batch_size: 16
  epochs: 100
  lr: 0.001 